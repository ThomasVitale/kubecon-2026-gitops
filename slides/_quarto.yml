project:
  type: default
  output-dir: _output

format:
  revealjs:
    theme: [default, custom.scss]
    slide-number: true
    transition: slide
    include-after-body: footer.html
    mermaid-format: svg
    mermaid:
      theme: neutral
